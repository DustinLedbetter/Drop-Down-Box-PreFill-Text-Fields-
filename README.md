# Drop-Down-Box-PreFill-Text-Fields-
This a javascript function: 

The function is called when a dropdown box choice is made. It fills text fields in with different data depending on the choice made.
